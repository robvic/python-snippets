import pyaudio
import wave

FORMAT = pyaudio.paInt16
CHANNELS = 1
RATE = 44100
CHUNK = 1024
RECORD_SECONDS = 5

p = pyaudio.PyAudio()

# Loop each stream

# Check errors

# Verify volume

# Pick one with highest volume